//
function jsCreateAcct() {
    try {
        //Instantiate variables
        var form = $('form');
        var btnSubmit = $('#btnSubmit');
    }
    catch (err) {
        console.log('ERROR: [jsCreateAcct] ' + err.message + ' | ' + err);
    }
}