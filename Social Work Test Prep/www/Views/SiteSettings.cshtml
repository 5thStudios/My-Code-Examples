@inherits Umbraco.Web.Mvc.UmbracoViewPage<ContentModels.SiteSettings>
@using ContentModels = Umbraco.Web.PublishedModels;
@{ Layout = null; }


@{
    //Redirect to home page.
    Response.Redirect("/", false);
}