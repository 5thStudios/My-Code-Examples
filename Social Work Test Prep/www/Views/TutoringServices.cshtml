@inherits Umbraco.Web.Mvc.UmbracoViewPage<ContentModels.TutoringServices>
@using ContentModels = Umbraco.Web.PublishedModels;
@{ Layout = "TopLevel.cshtml"; }


@section Header
{
    <script src="https://www.google.com/recaptcha/api.js?onload=renderRecaptcha&render=explicit" async defer></script>
}


<div class="grid-container Standard">
    <div class="titles">
        <br />
        @if (!string.IsNullOrWhiteSpace(Model.Title))
        {<h1>@Model.Title</h1> }
    else
    { <h1>@Model.Name</h1>}

        @if (!string.IsNullOrWhiteSpace(Model.Subtitle))
        {<h2>@Model.Subtitle</h2>}
        <br />
    </div>



    <div class="grid-x grid-margin-x grid-margin-y">
        <div class="cell laptop-14 large-14">

            <div class="main-content">


                @*@if (TempData.ContainsKey(bl.Models.Common.TempData.Success) && (bool)TempData[bl.Models.Common.TempData.Success] == true)
                {
                    <br />
                    <br />
                    <br />
                    <h3 class="text-center">Thank you for submitting your request.<br />We will respond to your request shortly.</h3>
                    <br />
                    <br />
                    <br />
                }
                else
                {
                    Html.RenderAction(bl.Models.Common.Action.RenderTutoringServices, bl.Models.Common.Controller.Member);
                }*@


                @Html.Raw(Model.Content)
            </div>
        </div>
        <div class="cell    laptop-9 laptop-offset-1   large-9 large-offset-1">
            @{Html.RenderAction(bl.Models.Common.Action.RenderSignupPanel, bl.Models.Common.Controller.Common);}
        </div>
    </div>
</div>
<br />
<br />